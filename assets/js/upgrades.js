/* --------------------------- 
Upgrade Handler
--------------------------- */
const upgradeCollection = [];

class Upgrade {
  constructor(upgradeName, upgradeModsItem, upgradePrice) {
    this.upgradeName = upgradeName;
    this.upgradeModsItem = upgradeModsItem;
    this.upgradePrice = upgradePrice;
  }
}
